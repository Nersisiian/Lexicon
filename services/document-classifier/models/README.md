# Document Classifier Model

Replace `classifier_model.pkl` with your trained model (e.g., scikit?learn pipeline).
The service expects the model to be at `/models/classifier_model.pkl` inside the container.

See `services/document-classifier/src/document_classifier/classifier.py` for loading logic.
