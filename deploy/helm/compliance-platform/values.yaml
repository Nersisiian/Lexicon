global:
  environment: dev
  imageRegistry: ghcr.io
  imageOwner: nersisiian
  imageRepo: lexicon
  kafkaBootstrapServers: kafka:9092

intake-gateway:
  enabled: true
  replicas: 1
  image: intake-gateway
  tag: latest
  databaseUrl: postgresql://compliance:${POSTGRES_PASSWORD:-compliance}@postgres:5432/intake
  redisUrl: redis://redis:6379
  minio:
    endpoint: minio:9000
    accessKey: ${MINIO_ACCESS_KEY:-minioadmin}
    secretKey: ${MINIO_SECRET_KEY:-minioadmin}
  service:
    port: 8000
  resources:
    limits:
      cpu: 500m
      memory: 512Mi
    requests:
      cpu: 200m
      memory: 256Mi

ocr-pipeline:
  enabled: true
  replicas: 1
  image: ocr-pipeline
  tag: latest
  env:
    OCR_BACKEND: paddle
  resources:
    limits:
      cpu: 1000m
      memory: 1.5Gi
    requests:
      cpu: 500m
      memory: 512Mi

document-classifier:
  enabled: true
  replicas: 1
  image: document-classifier
  tag: latest
  env:
    MODEL_NAME: finreg-bert-classifier
    DEVICE: cpu
  resources:
    limits:
      cpu: 1000m
      memory: 2Gi
    requests:
      cpu: 500m
      memory: 1Gi

compliance-engine:
  enabled: true
  replicas: 1
  image: compliance-engine
  tag: latest
  env:
    SANCTIONS_API_URL: http://sanctions-api:8080/search

fraud-detection:
  enabled: true
  replicas: 1
  image: fraud-detection
  tag: latest

reasoning-service:
  enabled: true
  replicas: 1
  image: reasoning-service
  tag: latest
  env:
    LLM_ENDPOINT: http://llm:8080/v1/completions

audit-ledger:
  enabled: true
  replicas: 1
  image: audit-ledger
  tag: latest
  databaseUrl: postgresql://compliance:${POSTGRES_PASSWORD:-compliance}@postgres:5432/audit
  service:
    port: 8000

review-workbench:
  enabled: true
  replicas: 1
  image: review-workbench
  tag: latest
  databaseUrl: postgresql://compliance:${POSTGRES_PASSWORD:-compliance}@postgres:5432/review
  service:
    port: 8000

notification-orchestrator:
  enabled: true
  replicas: 1
  image: notification-orchestrator
  tag: latest
  env:
    SLACK_WEBHOOK_URL: https://hooks.slack.com/services/...
