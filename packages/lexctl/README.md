# lexctl – Lexicon CLI

Command-line interface for the Lexicon compliance platform.

## Installation
```bash
pip install lexctl
Usage
bash
lexctl upload sample.pdf
lexctl status <document_id>
lexctl webhook subscribe --event document.completed --url http://example.com
